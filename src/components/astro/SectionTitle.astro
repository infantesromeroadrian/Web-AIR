---
interface Props { title: string; subtitle?: string; id?: string; center?: boolean; }
const { title, subtitle, id } = Astro.props;
---
<div class="section-heading" id={id}><h2>{title}</h2>{subtitle && <p>{subtitle}</p>}</div>
