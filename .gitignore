node_modules/
dist/
.astro/
.DS_Store
*.log
.env
.env.*
.claude/worktrees/
review-*.png
.playwright-mcp/
.vercel

# ARCA hooks symlink (local, no commitable)
hooks/
hooks

# Huion driver state files (regenerated at boot, never trackable)
.DriverUI.pid
.HuionCore.pid

# LinkedIn profile scraping (personal data, never to public repo)
linkedin/

# Engram MCP local state (chunks + manifest, regenerated per session)
.engram/
